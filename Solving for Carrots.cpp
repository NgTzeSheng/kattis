// The question seems long and complicated, but essentially, you just need to return the 2nd input. (trick question sort of)

#include <iostream>
#include <string>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    cout << b;
}
